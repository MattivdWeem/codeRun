module.exports = function (app, options, methods, Models, Controllers){

    var module = {}
    module.join = function(user, session){


        var Game = new options.models.Game;

        Game.findOneById


    }
    Models.gameModel = module;
}